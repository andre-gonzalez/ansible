- name: Clone slock
  tags:
    - slock-install
    - full-install
  ansible.builtin.git:
    repo: https://github.com/Andre-gonzalez/my-slock.git
    dest: "/home/frank/.config/slock"

- name: Install slock
  tags:
    - slock-install
    - full-install
  make:
    chdir: "/home/frank/.config/slock"
    target: install
  become: yes
