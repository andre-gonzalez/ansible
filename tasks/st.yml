- name: Clone st
  tags:
    - st-install
    - full-install
  ansible.builtin.git:
    repo: https://github.com/andre-gonzalez/my-st
    dest: "/home/{{ user }}/.config/st"

- name: Install st
  tags:
    - st-install
    - full-install
  make:
    chdir: "/home/{{ user }}/.config/st"
    target: install
  become: yes
