- name: Clone st
  tags:
    - st-install
    - full-install
  ansible.builtin.git:
    repo: https://github.com/andre-gonzalez/my-st
    dest: "/home/frank/.config/st"

- name: Install st
  tags:
    - st-install
    - full-install
  make:
    chdir: "/home/frank/.config/st"
    target: install
  become: yes
