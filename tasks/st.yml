- name: Clone st
  tags:
    - st-install
    - full-install
  ansible.builtin.git:
    repo: git@github.com:andre-gonzalez/my-st.git
    dest: "{{ lookup('env', 'HOME') }}/.config/st"
    accept_newhostkey: yes

- name: Install st
  tags:
    - st-install
    - full-install
  make:
    chdir: "{{ lookup('env', 'HOME') }}/.config/st"
    target: install
  become: yes
