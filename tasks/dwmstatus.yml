---
- name: Clone dwmstatus
  tags:
    - full-install
    - light-install
    - dwmblocks
  ansible.builtin.git:
    version: master
    repo: https://git.suckless.org/dwmstatus
    dest: "{{ ansible_env.HOME }}/.config/dwmstatus"
  ignore_errors: true

- name: Install dwmstatus
  tags:
    - full-install
    - light-install
    - dwmblocks
  community.general.make:
    chdir: "{{ ansible_env.HOME }}/.config/dwmstatus"
    target: install
  become: true
