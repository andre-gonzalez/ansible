- name: Clone dmenu
  tags:
    - dmenu-install
    - full-install
  git:
    repo: https://git.suckless.org/dmenu
    dest: "{{ user }}/.config"

- name: Install dmenu
  tags:
    - dmenu-install
    - full-install
  make:
    chdir: "{{ user }}/.config/dmenu"
    target: install
  become: yes
