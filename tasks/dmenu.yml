- name: Clone dmenu
  tags:
    - dmenu-install
    - full-install
  ansible.builtin.git:
    repo: https://git.suckless.org/dmenu
    dest: "/home/{{ user }}/.config/dmenu"

- name: Install dmenu
  tags:
    - dmenu-install
    - full-install
  make:
    chdir: "/home/{{ user }}/.config/dmenu"
    target: install
  become: yes
