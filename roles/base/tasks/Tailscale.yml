---
- name: Tailscale | Debian and related families
  tags:
    - tailscale
  when: ansible_facts["distribution"] in tailscale_debian_family_distros
  ansible.builtin.include_tasks: ./roles/base/tasks/tailscale-debian.yml

- name: Tailscale | Arch
  tags:
    - tailscale
  when: ansible_facts["distribution"] == 'Archlinux'
  ansible.builtin.include_tasks: ./roles/base/tasks/tailscale-arch.yml

- name: Tailscale | Remove legacy state folder
  tags:
    - tailscale
  ansible.builtin.file:
    path: "{{ ansible_env.HOME }}/.artis3n-tailscale"
    state: absent

- name: Tailscale | Determine state folder
  tags:
    - tailscale
  ansible.builtin.set_fact:
    # Following https://specifications.freedesktop.org/basedir-spec/basedir-spec-latest.html
    base_tailscale_state_folder: "{{ ansible_env.XDG_STATE_HOME | default(ansible_env.HOME + '/.local/state') }}"

- name: Tailscale | Set state idempotency folder
  tags:
    - tailscale
  ansible.builtin.file:
    path: "{{ item }}"
    state: directory
    owner: "{{ ansible_user_uid }}"
    group: "{{ ansible_user_gid }}"
    mode: "0700"
  loop:
    - "{{ base_tailscale_state_folder }}"
    - "{{ base_tailscale_state_folder }}/artis3n-tailscale"

- name: Tailscale | Store state idempotency README
  tags:
    - tailscale
  ansible.builtin.copy:
    src: files/state_readme.md
    dest: "{{ base_tailscale_state_folder }}/artis3n-tailscale/README.md"
    owner: "{{ ansible_user_uid }}"
    group: "{{ ansible_user_gid }}"
    mode: "0644"

- name: Tailscale | Enable Service
  tags:
    - tailscale
  become: true
  ansible.builtin.service:
    name: "{{ tailscale_service }}"
    state: started
    enabled: true

- name: Tailscale | Fetch Tailscale status
  tags:
    - tailscale
  ansible.builtin.command: tailscale status --json
  changed_when: false
  register: base_tailscale_status

- name: Tailscale | Parse status JSON
  tags:
    - tailscale
  vars:
    base_status: "{{ base_tailscale_status.stdout | from_json }}"
  ansible.builtin.set_fact:
    base_tailscale_is_online: "{{ base_status.Self.Online }}"
    base_tailscale_version: "{{ base_status.Version }}"

- name: Tailscale | Tailscale version and online status
  tags:
    - tailscale
  ansible.builtin.debug:
    msg: "Ver: {{ base_tailscale_version }} Online: {{ base_tailscale_is_online }}"
  when: verbose

- name: Tailscale | Save State
  tags:
    - tailscale
  ansible.builtin.template:
    src: state.j2
    dest: "{{ base_tailscale_state_folder }}/artis3n-tailscale/state"
    owner: "{{ ansible_user_uid }}"
    group: "{{ ansible_user_gid }}"
    mode: "0644"
  register: base_state_file

- name: Tailscale | Bring Tailscale Up
  tags:
    - tailscale
  become: true
  ansible.builtin.command: tailscale up {{ tailscale_args | trim }} --authkey={{ tailscale_authkey }}
  # Since the auth key is included in this task's output, we do not want to log output
  no_log: "{{ not (insecurely_log_authkey | bool) }}"
  changed_when: true
  register: base_tailscale_start
  # If a failure occurred due to state changes, we still want to log a redacted version of the error if "no_log" is true
  ignore_errors: true
  when:
    - not tailscale_up_skip
    - base_state_file is changed or not base_tailscale_is_online
  notify: Confirm Tailscale is Connected
  async: 60
  poll: 5

- name: Tailscale | Report non-sensitive stdout from "tailscale up" # noqa: no-handler
  tags:
    - tailscale
  ansible.builtin.debug:
    msg: "{{ base_tailscale_start.stdout | replace(tailscale_authkey, 'REDACTED') | regex_replace('\\t', '') | split('\n') }}"
  when:
    - base_tailscale_start is failed
    - base_tailscale_start.stdout | length > 0
  register: base_nonsensitive_stdout

- name: Tailscale | Pausing to highlight stdout message above
  tags:
    - tailscale
  ansible.builtin.pause:
    seconds: 5
  when: base_nonsensitive_stdout is not skipped

- name: Tailscale | Clear State Upon Error
  tags:
    - tailscale
  ansible.builtin.file:
    path: "{{ base_tailscale_state_folder }}/artis3n-tailscale/state"
    state: absent
  when:
    - base_tailscale_start is failed

- name: Tailscale | Report redacted failure from "tailscale up" # noqa: no-handler
  tags:
    - tailscale
  ansible.builtin.fail:
    msg: "{{ base_tailscale_start.stderr | default () | regex_replace('tskey.*\\s', 'REDACTED ') | regex_replace('\\t', '') | split('\n') }}"
  when:
    - base_tailscale_start is failed
